

if HAVE_GNOME
tracker_gui_dir = tracker-search-tool
else
tracker_gui_dir =
endif

SUBDIRS = libstemmer text-filters trackerd libtracker tracker-extract $(tracker_gui_dir)
DIST_SUBDIRS = libstemmer text-filters trackerd libtracker tracker-extract $(tracker_gui_dir)
